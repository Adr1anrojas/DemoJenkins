pipeline {
    agent any

    tools {nodejs "node"}

    environment {
        CHROME_BIN = '/bin/google-chrome'
    }

    stages {
        stage('Install Dependencies') {
            steps {
				bat 'cd FrontDemo'
                bat 'npm i'
            }
        }
        stage('Build Angular Proyect') {
            steps {
				bat 'cd FrontDemo'
                bat 'npm run build'
            }
        }
        stage('Cypress tests') {
            steps {
				bat 'cd FrontDemo'
                bat 'npm run open'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}